Scan this repository and update the cloud environment so Python3, pip, pytest,
pandas, pyyaml, plotly, streamlit, streamlit-autorefresh, kaleido,
websocket-client, and redis are preinstalled with editable install support.
Set startup script to:
  cd /workspace
  export PATH="$HOME/.local/bin:$PATH"
  python3 -m pip install -e '.[dev,dashboard,live]'
